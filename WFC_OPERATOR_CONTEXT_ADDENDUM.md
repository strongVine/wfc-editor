# Дополнение к WFC `rotationMap` как оператор

Адресовано ЛЛМ. Дополняет основной документ; не заменяет. Содержит три проработанных примера матрицы — от тривиального к сложному, с полным прогоном симметрии и геометрической проверкой через `GetMasks`. Терминологическая база — глоссарий §0 основного документа.

---

## A. Шкала примеров

Примеры выстроены лестницей возрастающей сложности по одному параметру — **ротации**, а не по числу состояний.

| Пример | Состояний | Где сложно |
|---|---|---|
| 1. Шахматка X/O | 2 | Нигде, все ниблы `0b1111`, ротация роли не играет |
| 2. Bead + Link  | 2 | Одно состояние с `r_self ≠ 0`, второе симметричное |
| 3. X/O с разными поворотами | 2 | Оба состояния с разными `r_self ≠ 0`, оба несимметричные |

Прохождение лестницы в порядке 1→2→3 даёт постепенное знакомство с переводом мировых описаний в локальные системы тайлов.

В примерах используется единая краткая форма прогона:

```
M[A, d] содержит B с битом r
   ⇒   d' = (opposite(d) − r + 4) % 4
       r0 = (4 − r) % 4
       проверить: M[B, d'] содержит A с битом r0
```

`opposite(d) = (d + 2) % 4`. Направления: 0=Front, 1=Right, 2=Back, 3=Left.

---

## B. Пример 1. Шахматка (поворот не значим)

**Условие.** Рядом с X — только O. Рядом с O — только X. Поворот соседа не ограничен (тайлы поворотно-симметричны, либо безразличны).

**Раскладка** (взгляд сверху, мировой Front вверх; r соседа любой, для наглядности r=0):

```
... X O X O X ...
... O X O X O ...
... X O X O X ...
... O X O X O ...
```

**Матрица:**

```csharp
//c0 X — окружён только O, поворот O любой
{
    //                X         O
    /*Front*/ _(0b0000, 0b1111),
    /*Right*/ _(0b0000, 0b1111),
    /*Back */ _(0b0000, 0b1111),
    /*Left */ _(0b0000, 0b1111),
    /*Up   */ _(0b0000, 0b0000),
    /*Down */ _(0b0000, 0b0000),
},
//c1 O — окружён только X, поворот X любой
{
    //                X         O
    /*Front*/ _(0b1111, 0b0000),
    /*Right*/ _(0b1111, 0b0000),
    /*Back */ _(0b1111, 0b0000),
    /*Left */ _(0b1111, 0b0000),
    /*Up   */ _(0b0000, 0b0000),
    /*Down */ _(0b0000, 0b0000),
},
```

**Прогон симметрии.** В каждой строке есть 4 живых бита, проверяю репрезентативные.

`M[X, Front, O] = 0b1111`, бит `r=0`:
- `d' = (2 − 0 + 4) % 4 = 2` → Back; `r0 = 0`
- `M[O, Back, X] = 0b1111` содержит бит 0. ✓

`M[X, Front, O] = 0b1111`, бит `r=1`:
- `d' = (2 − 1 + 4) % 4 = 1` → Right; `r0 = 3`
- `M[O, Right, X] = 0b1111` содержит бит 3. ✓

`M[X, Front, O] = 0b1111`, бит `r=2`:
- `d' = (2 − 2 + 4) % 4 = 0` → Front; `r0 = 2`
- `M[O, Front, X] = 0b1111` содержит бит 2. ✓

`M[X, Right, O] = 0b1111`, бит `r=3`:
- `d' = (3 − 3 + 4) % 4 = 0` → Front; `r0 = 1`
- `M[O, Front, X] = 0b1111` содержит бит 1. ✓

Нули `M[X, *, X]` и `M[O, *, O]` симметричны вакуумно.

**Геометрический прогон.** Тайл X в позиции (0,0,0), схлопнут в r=0. `directs = 0b0001` (один бит в нибле X). Применяю `GetMasks` для worldDirect=0 (Front):
- `selfStateIndex=0` (X), живой бит `selfDirect=0` (r=0)
- `relativeSelfDirect = (0 − 0 + 4) % 4 = 0` → строка `M[X, Front]` = `(0b0000, 0b1111)`
- `RotateMask(0b1111_0000, 0)` = без изменений = `(0b0000, 0b1111)`
- mask = `(0b0000 в нибле X, 0b1111 в нибле O)`

Сосед впереди должен быть в этой маске: O в любом из 4 поворотов. ✓ — соответствует условию.

---

## C. Пример 2. Bead + Link (одно состояние с нетривиальным поворотом)

**Условие.** `c0 Bead` — поворотно-симметричный шарик, физически без выраженного «фасада». `c1 Link` — соединительное звено-«гантелька» с двумя концами по локальной оси Front↔Back и глухими боками Left/Right.

**Желаемая раскладка.** Бусины и звенья чередуются вдоль одного измерения, образуя горизонтальные цепи; разные цепи параллельны и лежат бок к боку.

```
мировой Front ↑

   ... Bead ─0─ Bead ─0─ Bead ─0─ Bead ...
   ... Bead ─0─ Bead ─0─ Bead ─0─ Bead ...
   ... Bead ─0─ Bead ─0─ Bead ─0─ Bead ...
```

`─0─` — Link в r=1 (концы смотрят Right/Left, бока — Front/Back). Все цепи параллельны мировой оси Right.

**Сложность по сравнению с Примером 1.** В Примере 1 ниблы тривиальны (`0b1111`), и ротация неважна. Здесь Link физически направленный и стоит в `r_self = 1`. Это значит при заполнении строки `M[Link, ...]` нельзя думать в мировой системе — нужно перевести в локальную систему Link. Bead симметричен (`r_self = 0`), для него локальная = мировая.

### Шаг 1. Мировое окружение каждого состояния

Bead (везде в r=0):
| мир направление | сосед | r соседа в мире |
|---|---|---|
| Front (0) | Bead | 0 |
| Right (1) | Link | 1 |
| Back  (2) | Bead | 0 |
| Left  (3) | Link | 1 |

Link (везде в r=1):
| мир направление | сосед | r соседа в мире |
|---|---|---|
| Front (0) | Link | 1 |
| Right (1) | Bead | 0 |
| Back  (2) | Link | 1 |
| Left  (3) | Bead | 0 |

### Шаг 2. Перевод в локальные системы

Bead `r_self = 0` — локальная система = мировая, ниблы тривиальны.

Link `r_self = 1`:
- `d_local = (d_world − 1 + 4) % 4`
- `r_local_neighbor = (r_world_neighbor − 1 + 4) % 4`

| мир напр. | сосед, мир r | лок. напр. Link | лок. r соседа |
|---|---|---|---|
| Front (0) | Link, 1 | `(0−1+4)%4 = 3` Left  | `(1−1+4)%4 = 0` |
| Right (1) | Bead, 0 | `(1−1+4)%4 = 0` Front | `(0−1+4)%4 = 3` |
| Back  (2) | Link, 1 | `(2−1+4)%4 = 1` Right | `0` |
| Left  (3) | Bead, 0 | `(3−1+4)%4 = 2` Back  | `3` |

### Матрица

```csharp
//c0 Bead — в мире r=0, локальная = мировая
{
    //                Bead      Link
    /*Front*/ _(0b0001, 0b0000),  // спереди Bead(r=0)
    /*Right*/ _(0b0000, 0b0010),  // справа  Link(r=1)
    /*Back */ _(0b0001, 0b0000),  // сзади   Bead(r=0)
    /*Left */ _(0b0000, 0b0010),  // слева   Link(r=1)
    /*Up   */ _(0b0000, 0b0000),
    /*Down */ _(0b0000, 0b0000),
},
//c1 Link — в мире r=1
{
    //                Bead      Link
    /*Front*/ _(0b1000, 0b0000),  // лок.Front (мир Right) → Bead лок.r=3 → мир r=0
    /*Right*/ _(0b0000, 0b0001),  // лок.Right (мир Back)  → Link лок.r=0 → мир r=1
    /*Back */ _(0b1000, 0b0000),  // лок.Back  (мир Left)  → Bead лок.r=3 → мир r=0
    /*Left */ _(0b0000, 0b0001),  // лок.Left  (мир Front) → Link лок.r=0 → мир r=1
    /*Up   */ _(0b0000, 0b0000),
    /*Down */ _(0b0000, 0b0000),
},
```

### Прогон симметрии

`M[Bead, Front, Bead] = 0b0001`, бит `r=0`:
- `d' = (2 − 0 + 4) % 4 = 2` → Back; `r0 = 0`
- `M[Bead, Back, Bead] = 0b0001` содержит бит 0. ✓

`M[Bead, Right, Link] = 0b0010`, бит `r=1`:
- `d' = (3 − 1 + 4) % 4 = 2` → Back; `r0 = 3`
- `M[Link, Back, Bead] = 0b1000` содержит бит 3. ✓

`M[Bead, Left, Link] = 0b0010`, бит `r=1`:
- `d' = (1 − 1 + 4) % 4 = 0` → Front; `r0 = 3`
- `M[Link, Front, Bead] = 0b1000` содержит бит 3. ✓

`M[Link, Front, Bead] = 0b1000`, бит `r=3`:
- `d' = (2 − 3 + 4) % 4 = 3` → Left; `r0 = 1`
- `M[Bead, Left, Link] = 0b0010` содержит бит 1. ✓ (зеркало предыдущей)

`M[Link, Right, Link] = 0b0001`, бит `r=0`:
- `d' = (3 − 0 + 4) % 4 = 3` → Left; `r0 = 0`
- `M[Link, Left, Link] = 0b0001` содержит бит 0. ✓

`M[Link, Back, Bead] = 0b1000`, бит `r=3`:
- `d' = (0 − 3 + 4) % 4 = 1` → Right; `r0 = 1`
- `M[Bead, Right, Link] = 0b0010` содержит бит 1. ✓

Все живые биты симметричны.

### Геометрический прогон через `GetMasks`

Тайл Link в (0,0,0), схлопнут в r=1 → `directs` имеет один живой бит на позиции `1*4 + 1 = 5` → нибл Bead = `0b0000`, нибл Link = `0b0010`.

worldDirect=1 (Right):
- `selfStateIndex=1` (Link), `selfDirect=1` (живой бит)
- `relativeSelfDirect = (1 − 1 + 4) % 4 = 0` → строка `M[Link, Front]` = `(0b1000, 0b0000)`
- `RotateMask(0b1000, 1)`: нибл нетривиальный, сдвиг на 1 → `((0b1000 << 1) | (0b1000 >> 3)) & 0b1111` = `(0b10000 | 0b0001) & 0b1111` = `0b0001`
- mask: Bead-нибл `0b0001`, Link-нибл `0b0000` → справа должен быть Bead в мировом r=0. ✓

worldDirect=0 (Front):
- `relativeSelfDirect = (0 − 1 + 4) % 4 = 3` → `M[Link, Left]` = `(0b0000, 0b0001)`
- `RotateMask(0b0001, 1)` = `((0b0001 << 1) | (0b0001 >> 3)) & 0b1111` = `0b0010`
- mask: Link-нибл `0b0010` → впереди Link в мировом r=1. ✓

Совпадает с раскладкой.

### Чему учит

Симметрия оператора — это **не симметрия раскладки**, а согласованность утверждений между двумя локальными системами с разными `r_self`. В Примере 1 этого не было видно (все `r_self = 0`), здесь — становится первым нетривиальным навыком.

---

## D. Пример 3. X/O с разными поворотами (оба состояния несимметричные)

**Условие.** Тот же шахматный паттерн соседства, что в Примере 1 (впереди-сзади X — O, по бокам — X), но теперь **все X в r=3 (Left), все O в r=1 (Right)**. Это пример где **оба** состояния повёрнуты, и оба требуют перевода в локальную систему.

**Раскладка:**

```
мировой Front ↑

      ... O→ O→ O→ O→ ...
      ... ←X ←X ←X ←X ...
      ... O→ O→ O→ O→ ...
      ... ←X ←X ←X ←X ...
```

### Шаг 1. Мировое окружение центрального X (в r=3)

| мир направление | мир сосед | мир поворот соседа |
|---|---|---|
| Front (0) | O | 1 |
| Right (1) | X | 3 |
| Back  (2) | O | 1 |
| Left  (3) | X | 3 |

### Шаг 2. Перевод в локальную систему X

X в мире повёрнут на `r_self = 3`. Перевод:
- `d_local = (d_world − r_self + 4) % 4`
- `r_local = (r_world − r_self + 4) % 4`

| мир напр. | мир сосед, r | локальное напр. X | локальный r соседа |
|---|---|---|---|
| Front (0) | O, 1 | `(0−3+4)%4 = 1` Right | `(1−3+4)%4 = 2` |
| Right (1) | X, 3 | `(1−3+4)%4 = 2` Back  | `(3−3+4)%4 = 0` |
| Back  (2) | O, 1 | `(2−3+4)%4 = 3` Left  | `(1−3+4)%4 = 2` |
| Left  (3) | X, 3 | `(3−3+4)%4 = 0` Front | `(3−3+4)%4 = 0` |

Строки матрицы X получаются прямо из этой таблицы:
- `M[X, Front]` (= лок. Front, мир Left): X с битом 0 → `0b0001`
- `M[X, Right]` (= лок. Right, мир Front): O с битом 2 → `0b0100`
- `M[X, Back]`  (= лок. Back, мир Right): X с битом 0 → `0b0001`
- `M[X, Left]`  (= лок. Left, мир Back): O с битом 2 → `0b0100`

### Шаг 3. Аналогично для O (в r=1)

Мировое окружение центрального O:

| мир направление | мир сосед | мир поворот соседа |
|---|---|---|
| Front (0) | X | 3 |
| Right (1) | O | 1 |
| Back  (2) | X | 3 |
| Left  (3) | O | 1 |

`r_self = 1`:

| мир напр. | мир сосед, r | локальное напр. O | локальный r соседа |
|---|---|---|---|
| Front (0) | X, 3 | `(0−1+4)%4 = 3` Left  | `(3−1+4)%4 = 2` |
| Right (1) | O, 1 | `(1−1+4)%4 = 0` Front | `(1−1+4)%4 = 0` |
| Back  (2) | X, 3 | `(2−1+4)%4 = 1` Right | `(3−1+4)%4 = 2` |
| Left  (3) | O, 1 | `(3−1+4)%4 = 2` Back  | `(1−1+4)%4 = 0` |

Строки:
- `M[O, Front]` (= лок. Front, мир Right): O с битом 0 → `0b0001`
- `M[O, Right]` (= лок. Right, мир Back): X с битом 2 → `0b0100`
- `M[O, Back]`  (= лок. Back, мир Left): O с битом 0 → `0b0001`
- `M[O, Left]`  (= лок. Left, мир Front): X с битом 2 → `0b0100`

### Матрица

```csharp
//c0 X — в мире повёрнут на r=3 (смотрит Left)
{
    //                X         O
    /*Front*/ _(0b0001, 0b0000),  // лок.Front (мир Left)  → X лок.r=0  → мир r=3
    /*Right*/ _(0b0000, 0b0100),  // лок.Right (мир Front) → O лок.r=2  → мир r=1
    /*Back */ _(0b0001, 0b0000),  // лок.Back  (мир Right) → X лок.r=0  → мир r=3
    /*Left */ _(0b0000, 0b0100),  // лок.Left  (мир Back)  → O лок.r=2  → мир r=1
    /*Up   */ _(0b0000, 0b0000),
    /*Down */ _(0b0000, 0b0000),
},
//c1 O — в мире повёрнут на r=1 (смотрит Right)
{
    //                X         O
    /*Front*/ _(0b0000, 0b0001),  // лок.Front (мир Right) → O лок.r=0  → мир r=1
    /*Right*/ _(0b0100, 0b0000),  // лок.Right (мир Back)  → X лок.r=2  → мир r=3
    /*Back */ _(0b0000, 0b0001),  // лок.Back  (мир Left)  → O лок.r=0  → мир r=1
    /*Left */ _(0b0100, 0b0000),  // лок.Left  (мир Front) → X лок.r=2  → мир r=3
    /*Up   */ _(0b0000, 0b0000),
    /*Down */ _(0b0000, 0b0000),
},
```

### Прогон симметрии

`M[X, Front, X] = 0b0001`, бит `r=0`:
- `d' = (2 − 0 + 4) % 4 = 2` → Back; `r0 = 0`
- `M[X, Back, X] = 0b0001` содержит бит 0. ✓

`M[X, Right, O] = 0b0100`, бит `r=2`:
- `d' = (3 − 2 + 4) % 4 = 1` → Right; `r0 = (4−2)%4 = 2`
- `M[O, Right, X] = 0b0100` содержит бит 2. ✓

`M[X, Back, X] = 0b0001`, бит `r=0`:
- `d' = (0 − 0 + 4) % 4 = 0` → Front; `r0 = 0`
- `M[X, Front, X] = 0b0001` содержит бит 0. ✓ (зеркало первой)

`M[X, Left, O] = 0b0100`, бит `r=2`:
- `d' = (1 − 2 + 4) % 4 = 3` → Left; `r0 = 2`
- `M[O, Left, X] = 0b0100` содержит бит 2. ✓

`M[O, Front, O] = 0b0001`, бит `r=0`:
- `d' = 2 → Back`; `r0 = 0`
- `M[O, Back, O] = 0b0001` содержит бит 0. ✓

`M[O, Right, X] = 0b0100`, бит `r=2`:
- `d' = (3 − 2 + 4) % 4 = 1` → Right; `r0 = 2`
- `M[X, Right, O] = 0b0100` содержит бит 2. ✓ (зеркало пары X↔O вправо)

`M[O, Back, O] = 0b0001`, бит `r=0`:
- `d' = 0 → Front`; `r0 = 0`
- `M[O, Front, O] = 0b0001` содержит бит 0. ✓

`M[O, Left, X] = 0b0100`, бит `r=2`:
- `d' = (1 − 2 + 4) % 4 = 3` → Left; `r0 = 2`
- `M[X, Left, O] = 0b0100` содержит бит 2. ✓

Все живые биты симметричны. Оператор корректен.

### Геометрический прогон через `GetMasks`

Тайл X в (0,0,0), схлопнут в r=3 → `directs` имеет один живой бит на позиции `0*4 + 3 = 3` → `directs = 0b1000` (только нибл состояния X, бит 3).

Проверю все 4 горизонтальных направления.

**worldDirect=0 (Front):**
- `selfStateIndex=0` (X), `selfDirect=3` (живой бит)
- `relativeSelfDirect = (0 − 3 + 4) % 4 = 1` → строка `M[X, Right]` = `(0b0000, 0b0100)`
- `RotateMask(0b0100, 3)`: нибл `0b0100` (нетривиальный), сдвиг на 3 → `((0b0100 << 3) | (0b0100 >> 1)) & 0b1111` = `(0b100000 | 0b010) & 0b1111` = `0b0010`
- Применяется только к ниблу O (нибл X нулевой), результат = X-нибл `0b0000`, O-нибл `0b0010`
- mask = O с битом 1 → O в мире r=1. ✓ (соответствует O в раскладке)

**worldDirect=1 (Right):**
- `relativeSelfDirect = (1 − 3 + 4) % 4 = 2` → `M[X, Back]` = `(0b0001, 0b0000)`
- `RotateMask(0b0001, 3)` = `((0b0001 << 3) | (0b0001 >> 1)) & 0b1111` = `(0b1000 | 0b0) & 0b1111` = `0b1000`
- mask = X с битом 3 → X в мире r=3. ✓

**worldDirect=2 (Back):**
- `relativeSelfDirect = (2 − 3 + 4) % 4 = 3` → `M[X, Left]` = `(0b0000, 0b0100)`
- `RotateMask(0b0100, 3)` = `0b0010` (как выше)
- mask = O с битом 1 → O в мире r=1. ✓

**worldDirect=3 (Left):**
- `relativeSelfDirect = (3 − 3 + 4) % 4 = 0` → `M[X, Front]` = `(0b0001, 0b0000)`
- `RotateMask(0b0001, 3)` = `0b1000`
- mask = X с битом 3 → X в мире r=3. ✓

Все четыре мировых направления дают именно тех соседей, что нарисованы в раскладке. Оператор задаёт паттерн.

### Где в этом примере легче всего ошибиться

1. **Перепутать локальное и мировое направление при заполнении строк.** Строка `M[X, Front]` описывает соседа в **локальном** Front тайла X. Если X сам повёрнут на r=3, его локальный Front смотрит в мировой Left. Сосед, попадающий в эту строку — тот, что в **мире** слева от X. Кто пишет матрицу глядя на мировую раскладку, должен сначала перевести в локальную систему, как в Шаге 2.

2. **Перепутать поворот соседа в мире и его поворот в локальной системе родителя.** В этом примере O в мире имеет r=1. С точки зрения X (который сам в r=3) тот же O видится как имеющий поворот `(1 − 3 + 4) % 4 = 2`. В матрицу X надо записать **локальный** поворот O — то есть бит 2.

3. **Принять `M[X, Front]` за «что у X спереди в мире»** при чтении готовой матрицы. Это неверно: матрица всегда говорит про локальную систему базово-ориентированного A. Чтобы узнать, что в мире впереди X в r=3, надо взять `M[X, Right]` (потому что (0−3+4)%4 = 1 = Right) и применить `RotateMask(_, 3)`.

Эти три ошибки — главный источник асимметрии, которую потом ловит проверка из §7 основного документа.

---

## E. Сводка: алгоритм заполнения матрицы под заданный паттерн

Когда пользователь описывает желаемую раскладку («все X смотрят туда, по бокам стоят такие-то...»), порядок шагов:

1. **Зафиксировать раскладку** в мировых терминах: для каждого состояния — в каком повороте оно стоит и какие у него мировые соседи (направление, состояние, поворот).
2. **Для каждого состояния A** определить его собственный мировой поворот `r_self`.
3. **Перевести 4 мировых соседа A в локальную систему A:**
   - `d_local = (d_world − r_self + 4) % 4`
   - `r_local_neighbor = (r_world_neighbor − r_self + 4) % 4`
4. **Заполнить строки** `M[A, d_local]`: в нибле состояния соседа выставить бит `r_local_neighbor`.
5. **Прогнать симметрию** — для каждого живого бита по формуле из §7. Любая асимметрия — повод спросить пользователя, какое из утверждений отражает физику (не «дописать»).
6. **Геометрический прогон через `GetMasks`** на одном схлопнутом тайле — убедиться, что мировые соседи получаются именно те, что в раскладке.

Если пользователь меняет повороты состояний (как в переходе от Примера 1 к Примеру 3), но соседство в мире остаётся прежним — содержимое строк матрицы меняется (потому что меняется перевод в локальные системы). Это ожидаемо, не баг.

**Сноска про инструменты.** При работе через редактор матрицы с автодополнением симметрии шаги 4 и 5 сливаются в один: пользователь кликает один бит, инструмент атомарно ставит зеркальный по формуле из §7 (см. также основной документ §7, режим B). Шаги 1-3 и 6 при этом остаются за пользователем — это семантика и проверка, а не механика.
